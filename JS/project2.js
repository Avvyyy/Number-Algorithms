
    let numbers = [];

    for (i = 0; i <= 24; i++){
         num = Math.pow(i , 2);
         numbers.push(i, num)
        document.getElementById('square').textContent = ("Number, square:" + " " + (numbers));
        
    }


